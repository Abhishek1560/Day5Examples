#include <iostream>
using namespace std;
int main() {

int side, vol;
       cout<<"Input the side of a cube: ";
           cin>> side;
           vol=(side*side*side);
        cout<<" The volume of cube is: "<<vol <<endl;

return 0;
}
